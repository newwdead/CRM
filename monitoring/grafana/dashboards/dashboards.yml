# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  - name: 'BizCard CRM Dashboards'
    orgId: 1
    folder: 'BizCard CRM'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false

